'use strict';
const elements=['화염','번개','냉기','대지','암흑'];
const colors=['#f1936e','#e7ce6a','#83d6ed','#bbaa79','#c39ae8'];
const names=['화염탄','번개탄','얼음창','바위창','그림자 구체'];
const effects=[
['기본 화염탄 · 피해 6 / 0.8초','피해 +15%','3중첩 점화 · 피해 12 / 반경 1.3','쿨다운 -10%','점화·사망 시 표식 2명 전염','관통 +1','점화 피해 +25% / 반경 +20%','2초 불장판 · 0.5초마다 피해 4'],
['기본 번개탄 · 피해 5 / 0.7초','피해 +15%','3번째 적중마다 2명에게 60% 연쇄','쿨다운 -10%','3중첩 방전 · 피해 10 / 반경 1.2','연쇄 대상 +1','방전 피해 +25%','6초마다 표식 적 3명 낙뢰 · 피해 12'],
['기본 얼음창 · 피해 5 / 0.9초 / 관통 1','피해 +15%','3중첩 시 0.6초 빙결','쿨다운 -10%','빙결 적 피격 시 파괴 · 피해 8 / 반경 1.2','관통 +1 / 둔화 효과 +10%','빙결 +0.3초','빙결 해제 지점 2.5초 장판 / 둔화 35%'],
['기본 바위창 · 피해 8 / 1.1초 / 밀치기','피해 +15%','3번째 적중 충격파 · 피해 10 / 반경 1.5','쿨다운 -10%','2초 지진 구역 / 둔화 30%','관통 +1 / 충격파 반경 +15%','밀치기 거리 +30%','5번째 공격마다 낙석 3개 / 피해 14'],
['기본 그림자 구체 · 피해 6 / 1초 / 관통 1','피해 +15%','3중첩 대상 받는 피해 +15%','쿨다운 -10%','3중첩 적 사망 시 3명 표식 전염','관통 +1','피해 증가 효과 +10%','HP 10% 이하 일반 적 처형']];
const pages=['시작','전투 HUD','스킬트리','레벨업','엘리트','보스','결과'];
const issues=[
['오각형 연결 충돌','문서 그림과 텍스트 연결 순서가 다르다.','텍스트 순서로 목업을 배치했다. 그림 수정 여부를 확정한다.'],
['카드 수·추첨','후보 4~5개 중 선정 방식과 후보 1~2개 표시 방식이 없다.','목업은 순서대로 최대 3개만 보여준다. 실제 가중치는 미정이다.'],
['인접 제한 우회','“8레벨 미만 원소 카드”가 잠긴 원소를 포함할 수 있다.','보유 또는 인접 원소만 후보로 제한할지 확정한다.'],
['성장 속도','8분 플레이어 레벨 12~16, 원소 2~4개와 각성 목표의 관계가 없다.','분배별 각성 도달을 검토한다. 무료 시작 1회 외 상승은 비용 1로 가정한다.'],
['표식 재발동','3중첩 발동 뒤 소비·유지·재발동 조건이 없다.','소비 후 재축적 또는 재발동 대기시간 중 하나를 정한다.'],
['연쇄의 연쇄','폭발·전염·연쇄가 새 표식과 고유 효과를 발동하는지 미정이다.','2차 효과의 표식 부여 여부와 연속 발동 상한을 정한다.'],
['빠진 전투 수치','냉기 기본 둔화·밀치기 거리·연쇄 거리·증가율 합산 방식이 없다.','레벨별 최종값과 기본 공격/부가 효과 적용 범위를 명시한다.'],
['냉기 보스 대응','빙결 면역 보스에게 냉기 3·5·7·8레벨 효과가 대부분 막힌다.','보스 전용 대체 피해 또는 부하를 통한 간접 효과를 검토한다.'],
['엘리트 행동 경계','생성 위치·제어 저항·소환 거리·소환자 사망 후 부하 처리가 없다.','예고 중 제어 효과와 부하 유지 여부를 결정한다.'],
['보스 패턴 경계','기본 이동속도·소환 상한·동시 패턴·페이즈 전환 중 공격 처리가 없다.','패턴 우선순위와 전환 시 기존 예고 유지 여부를 정한다.'],
['동시 승패','보스와 플레이어 사망, 10분 도달이 겹칠 때 우선순위가 없다.','승리/사망/시간 초과 처리 순서를 명시한다.'],
['화면 흐름','일시정지·트리 열람·다중 레벨업·결과 표시 항목이 없다.','목업의 큐·열람·결과 표시를 검토하고 실제 규칙을 확정한다.'],
['담당표 누락','이선동의 매일 전체 에셋 통합 역할이 기획서에 명시되지 않았다.','선동 에셋 통합 / 유신 전투 코드 / 승범 UI 코드 / 태환 QA를 문서화한다.']];
const pageIssues=[[12,11],[4,5,6,3],[0,2,3],[1,2,3,11],[8,4],[7,9,10],[10,11]];
let state;
const $=id=>document.getElementById(id);
function reset(){state={page:0,levels:[0,0,0,0,0],selected:0,time:0,hp:100,queue:0,elite:'돌진 예고',boss:'등장',result:'승리',tie:false,note:''};render();}
function eligible(i){return state.levels[i]<8&&(state.levels[i]>0||state.levels[(i+4)%5]>0||state.levels[(i+1)%5]>0);}
function candidates(){return elements.map((_,i)=>i).filter(eligible).slice(0,3);}
function spent(){return Math.max(0,state.levels.reduce((a,b)=>a+b,0)-1);}
function assume(text){return `<p class="assumption">목업 가정 · ${text}</p>`;}
function action(text,act,val='',cls=''){return `<button class="${cls}" data-act="${act}" data-val="${val}">${text}</button>`;}
function heading(title,sub){return `<div class="eyebrow">${pages[state.page]} / 0${state.page+1}</div><h2>${title}</h2><p>${sub}</p>`;}
function hud(){return `<div class="hud"><span>HP 80 / 100<div class="bar"><i style="width:80%"></i></div></span><span>시간 ${Math.floor(state.time/60)}:${String(state.time%60).padStart(2,'0')}</span><span>플레이어 Lv.${spent()+1}<br>EXP 3 / 10 (표시 예시)</span></div>`;}
function skills(){return `<div class="row">${elements.map((e,i)=>`<span class="tag" style="color:${colors[i]}">${e} ${state.levels[i]?`Lv.${state.levels[i]}`:'미보유'}</span>`).join('')}</div>`;}
function arena(kind){let actors='<div class="actor" style="left:30%;top:65%">마법사</div>';if(kind==='combat')actors+='<div class="actor enemy" style="left:68%;top:40%">일반 적<br>표식 ●●○</div><div class="actor enemy" style="left:80%;top:70%">빠른 적</div>';else if(kind==='elite'){actors+=`<div class="actor enemy" style="left:${state.elite==='돌진'?'40':'70'}%;top:40%">${state.elite.includes('돌진')?'돌진자':'소환술사'}</div>`;actors+=state.elite==='돌진 예고'?'<div class="telegraph"></div>':state.elite==='소환 예고'?'<div class="ring"></div>':state.elite==='소환 완료'?'<div class="bolts">◆ ◆ ◆</div>':'';}else{actors+='<div class="actor boss" style="left:50%;top:35%">원소 수호자</div>';actors+=state.boss==='충격파'?'<div class="ring"></div>':state.boss==='투사체'?`<div class="bolts">${state.hp<=70?'↙ ↓ ↓ ↓ ↘':'↙ ↓ ↘'}</div>`:state.boss==='소환'?'<div class="bolts">◆ ◆</div>':'';}return `<div class="arena">${actors}</div>`;}
function screen(){switch(state.page){case 0:return heading('어떤 원소로 시작할까?','하나를 고르면 1레벨을 무료로 얻는다.')+`<div class="cards">${elements.map((e,i)=>`<article class="card" style="--element:${colors[i]}"><h3>${e}</h3><p>${names[i]}</p><p>${effects[i][0]}</p>${action('이 원소로 시작','start',i)}</article>`).join('')}</div>`+assume('시작 선택은 목업의 성장 상태를 초기화한다.');
case 1:return heading('전투 중 무엇을 알아야 할까?','이름 붙인 도형은 외형 자리표시자다. 실제 전투는 실행하지 않는다.')+hud()+arena('combat')+skills()+`<div class="actions">${action('레벨업 보기','level')}${action('트리 열람','page',2)}</div>`+assume('HP·EXP는 표시 예시. 화면 전환 중 시간은 자동으로 흐르지 않는다.');
case 2:{let positions=[[50,12],[88,39],[74,84],[26,84],[12,39]];return heading('다섯 원소, 하나의 성장 지도','꼭짓점을 누르면 잠금 이유와 성장 효과를 확인한다.')+`<div class="layout"><div class="pentagon"><svg viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true"><polygon points="50,12 88,39 74,84 26,84 12,39" fill="none" stroke="#788696" stroke-width=".7"/></svg>${elements.map((e,i)=>`<button class="node" style="left:${positions[i][0]}%;top:${positions[i][1]}%;--element:${colors[i]}" data-act="inspect" data-val="${i}">${e} Lv.${state.levels[i]}<em>${state.levels[i]===8?'최대 레벨':eligible(i)?'성장 가능':'인접 원소 필요'}</em></button>`).join('')}</div><article class="card"><h3>${elements[state.selected]} · Lv.${state.levels[state.selected]}</h3><p>${state.levels[state.selected]===8?'최대 레벨이다.':eligible(state.selected)?`다음: ${effects[state.selected][state.levels[state.selected]]}`:'잠금: 이웃 원소 중 하나를 먼저 보유해야 한다.'}</p>${[3,5,8].map(l=>`<p><b>Lv.${l}</b> ${effects[state.selected][l-1]}</p>`).join('')}</article></div><p>사용 성장 횟수 ${spent()} · 플레이어 레벨 ${spent()+1} 가정</p>`+assume('트리는 열람만 한다. 실제 상승은 레벨업 카드에서 선택한다.');}
case 3:{let pool=candidates();return heading('이번 레벨업에서 무엇을 고를까?',`대기 선택 ${state.queue}회 · 후보 ${elements.filter((_,i)=>eligible(i)).length}개`)+skills()+(!state.levels.some(Boolean)?`<p>시작 원소를 먼저 선택한다.</p>${action('시작 화면','page',0)}`:!pool.length?'<article class="card"><h3>모든 원소 Lv.8</h3><p>기획: 레벨업 창을 열지 않고 최대 HP 10% 회복.</p><p>이 화면은 검토용이다. 실제 HP 변경은 실행하지 않는다.</p></article>':`<div class="cards">${pool.map(i=>`<article class="card" style="--element:${colors[i]}"><h3>${elements[i]} Lv.${state.levels[i]} → ${state.levels[i]+1}</h3><p>${effects[i][state.levels[i]]}</p>${action(state.queue?'선택 적용':'검토용 레벨업 추가 필요','pick',i)}<p>주요 변화: 3 · 5 · 8레벨</p></article>`).join('')}</div>`)+`<div class="actions">${action('검토용 레벨업 +1','level')}</div>`+assume('유효 후보 중 오각형 순서로 최대 3개 표시. 후보 부족 시 실제 수만 표시. 선택 즉시 적용하고 남은 큐가 있으면 다음 카드 표시. 추첨·확정 방식은 미정.');}
case 4:return heading('예고를 보고 피할 수 있을까?',`${state.elite} · 3분 돌진자 / 6분 소환술사`)+hud()+arena('elite')+`<div class="actions">${['돌진 예고','돌진','소환 예고','소환 완료'].map(v=>action(v,'elite',v,state.elite===v?'active':'')).join('')}</div><p>돌진: 예고 1.2초 → 돌진 0.8초 / 8초 주기<br>소환: 예고 0.8초 → 3마리 / 최대 6마리 / EXP 없음</p>`+assume('위치·방향·도형 크기는 예시이며 단계는 버튼으로 넘긴다.');
case 5:return heading('원소 수호자',`${state.hp<=70?'2페이즈 · 폭주':'1페이즈 · 수호'} / 검토 단계: ${state.boss}`)+`<div class="hud">보스 HP ${state.hp}% · ${state.hp*20} / 2,000<div style="flex:1" class="bar"><i style="width:${state.hp}%"></i></div></div>`+arena('boss')+`<div class="actions">${['등장','1페이즈','70% 전환','2페이즈','투사체','충격파','소환'].map(v=>action(v,'boss',v,state.boss===v?'active':'')).join('')}</div><p>1페이즈: 4.5초마다 3발 / 20도 간격 / 피해 10<br>2페이즈: 4초마다 5발 · 충격파 5초 주기 / 예고 0.8초 · 10초마다 2마리 소환</p>`+assume('정확히 HP 70%부터 2페이즈로 표시. 패턴은 개별 미리보기이며 중첩·공격 판정은 재현하지 않는다.');
default:return heading('한 판의 끝','결과 정보와 경계 우선순위를 검토한다.')+`<div class="actions">${['승리','사망','시간 초과'].map(v=>action(v,'result',v)).join('')}</div><div class="outcome"><div class="eyebrow">RUN RESULT</div><h2>${state.tie?'판정 미정':state.result}</h2><p>${state.tie?'보스 HP 0 + 플레이어 HP 0 + 10:00 도달<br>우선순위가 없어 결과를 선택하지 않는다.':state.result==='승리'?'보스 처치 → 즉시 승리':state.result==='사망'?'플레이어 사망 → 패배':'10:00 보스 생존 → 패배'}</p>${skills()}<p>성장 횟수 ${spent()} · 표시 항목은 제안</p>${action('재시작','reset','','primary')}</div>`;}}
function render(){ $('nav').innerHTML=pages.map((p,i)=>action(p,'page',i,state.page===i?'active':'')).join('');$('screen').innerHTML=screen();$('review').innerHTML=`<span class="eyebrow">REVIEW NOTES</span><h2>이 화면의 빈 규칙</h2><h3>확정 내용</h3><p>5원소 · 최대 8레벨 · 기존 28노드 미사용. 상세 수치는 기획 원본을 따른다.</p><h3>누락·충돌 / 제안</h3>${pageIssues[state.page].map(i=>`<div class="issue"><b>${String(i+1).padStart(2,'0')} · ${issues[i][0]}</b>${issues[i][1]}<p>제안: ${issues[i][2]}</p></div>`).join('')}<details><summary>전체 누락 ${issues.length}개 보기</summary>${issues.map((x,i)=>`<p>${i+1}. ${x[0]} — ${x[1]}</p>`).join('')}</details>`;$('time').value=String(state.time);$('hp').value=state.hp;$('hpLabel').textContent=state.hp+'%';$('notice').textContent=state.note;}
function perform(act,val){state.note='';switch(act){case 'reset':reset();return;case 'page':state.page=Number(val);break;case 'inspect':state.selected=Number(val);break;case 'start':state.levels=[0,0,0,0,0];state.levels[Number(val)]=1;state.selected=Number(val);state.queue=0;state.time=0;state.hp=100;state.tie=false;state.page=1;break;case 'level':state.page=3;if(state.levels.some(Boolean)&&state.levels.some(l=>l<8))state.queue++;else state.note='시작 원소가 없거나 모든 원소가 만렙이다.';break;case 'pick':{let i=Number(val);if(state.queue>0&&candidates().includes(i)){state.levels[i]++;state.queue--;state.note=`${elements[i]} Lv.${state.levels[i]} 적용 (목업 상태)`;if(!state.queue)state.page=1;}break;}case 'elite':state.elite=val;state.time=val.includes('돌진')?180:360;break;case 'boss':state.boss=val;if(val==='등장'||val==='1페이즈')state.hp=100;else if(['70% 전환','2페이즈','충격파','소환'].includes(val))state.hp=70;state.time=480;break;case 'result':state.result=val;state.tie=false;break;}render();}
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.id==='reset')return reset();if(b.dataset.act)return perform(b.dataset.act,b.dataset.val);if(b.dataset.case){state.note='경계 상황을 위한 목업 프리셋. 실제 플레이 결과가 아니다.';switch(b.dataset.case){case 'few':state.levels=[8,8,8,8,7];state.queue=1;state.page=3;break;case 'two':state.levels=[8,8,8,7,7];state.queue=1;state.page=3;break;case 'max':state.levels=[8,8,8,8,8];state.queue=0;state.page=3;break;case 'phase':state.page=5;state.hp=70;state.time=480;state.boss='70% 전환';break;case 'tie':state.page=6;state.time=600;state.hp=0;state.tie=true;break;case 'queue':state.levels=[1,0,0,0,0];state.queue=3;state.page=3;break;}render();}});
$('time').addEventListener('change',e=>{state.time=Number(e.target.value);state.page=state.time===0?0:state.time<480?4:state.time===480?5:6;if(state.page===4)state.elite=state.time===180?'돌진 예고':'소환 예고';if(state.time===600){state.result='시간 초과';state.tie=false;}render();});
$('hp').addEventListener('input',e=>{state.hp=Number(e.target.value);state.page=5;state.boss=state.hp<=70?'2페이즈':'1페이즈';state.note=state.hp===0?'HP 0은 검토용 수치. 승패 자동 판정은 하지 않는다.':'';render();});
reset();
